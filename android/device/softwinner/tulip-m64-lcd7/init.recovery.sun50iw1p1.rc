on init

    insmod /sw-device.ko debug_mask=0xff ctp_mask=1
    mkdir /usbhost
    mkdir /extsd
    mkdir /bootloader

service console /system/bin/sh
    class core
    console
    disabled
    user shell
    group shell log
    seclabel u:r:shell:s0
